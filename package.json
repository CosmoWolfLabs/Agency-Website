{
  "name": "cosmowolf-labs-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspace=web && rm -rf ./.next ./public ./next.config.* && cp -r apps/web/.next ./.next && cp -r apps/web/public ./public && cp apps/web/next.config.* ./",
    "build:web": "npm run build --workspace=web && rm -rf ./.next ./public ./next.config.* && cp -r apps/web/.next ./.next && cp -r apps/web/public ./public && cp apps/web/next.config.* ./",
    "build:admin": "npm run build --workspace=admin && rm -rf ./.next ./public ./next.config.* && cp -r apps/admin/.next ./.next && cp -r apps/admin/public ./public && cp apps/admin/next.config.* ./"
  }
}
